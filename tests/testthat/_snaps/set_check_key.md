# key_exists() output is stable

    API key not set. Use set_key() to store API key before making a request.

---

    Code
      key_exists()
    Message
      An existing JotForm API key is stored in .Rprofile.

